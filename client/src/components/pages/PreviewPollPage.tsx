import React, {FC, Fragment} from "react";
import InitialPoll from "../../models/InitialPoll";

const PreviewPollPage: FC = () => {
    return (<Fragment>
        Page is currently in develop
    </Fragment>)
}

export default PreviewPollPage